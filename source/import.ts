/**
 * @file This file is used as the entrypoint, where all other files are imported into (whether directly, or through files that use them).
 * @author James Holtom
 */

import "./game/core";
import "./game/blockly";
import "./game/phaser";
import "./game/tester";
import "./styles";
